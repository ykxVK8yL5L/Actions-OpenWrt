name: test trigger self 

on:
  workflow_call:
  workflow_dispatch:
    inputs:
        finish:
          type: boolean
          description: "是否结束工作流"

env:
  FINISH: ${{ inputs.finish }}

jobs:
  build:
    runs-on: ubuntu-latest   
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: 'test'
        run: |
         echo "hello"
  
  trigger:
    runs-on: ubuntu-latest
    needs: build 
    if: github.event.inputs.finish == 'false'

    steps:
      - name: Checkout
        uses: actions/checkout@main
      
      - name: trigger
        uses: ykxVK8yL5L/Actions-OpenWrt/.github/workflows/test.yml@main
        with:
          finish: true

